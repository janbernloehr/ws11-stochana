dist: trusty
sudo: required

before_install:
- sudo add-apt-repository ppa:jonathonf/texlive-2016 -y
- sudo apt-get update -q
- sudo apt-get install --no-install-recommends texlive-fonts-recommended texlive-latex-extra texlive-fonts-extra texlive-latex-recommended texlive-lang-german dvipng latexmk

script:
- mkdir _build
- latexmk -pdf StochAna.tex
#
#sudo: required
#
#language: ruby
#
#services:
#  - docker
#
#before_install:
#  - docker build -t texlive docker
#  - docker run -it -v /var/texlive texlive sh -c "pdflatex document.lex"
#  - docker ps -a